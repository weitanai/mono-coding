const nullObj = null;

console.log('null to string:', String(null), String(nullObj).length);
console.log('null to boolean:', Boolean(nullObj));
console.log('null to number:', Number(null));
console.log('null to Object:', Object(null));
